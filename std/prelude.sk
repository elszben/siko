module Prelude where

not :: Bool -> Bool
not a = if a then False
             else True

print :: String -> ()
print msg = extern

println :: String -> ()
println msg = extern

data List a = extern

class Show a where
    show a :: a -> String

instance as ListShow (Show a) => Show List a where
    show a = extern
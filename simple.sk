module Main where

foo a = a

data Foo = Foo Int deriving (Show)

main  = do
        foo (4 + 4)
        println "alma"
        println 4
        println "{}" % Foo 1
        println (Foo 1)
        println "{}" % True
        println "{}" % -1.0
        ()
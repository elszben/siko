module alma.korte (main, Person, Person(.., Alma)) where

import Alma hiding (alm, a)
 
import korte (szilva, kd(a,a))

data Option a = Some a | None a korte.szilva

data Person = Person { name :: Alma.String, address :: Option String }

main :: korte.szilva
main = 5
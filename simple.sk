module Main where

/*
protocol BazProtocol = Baz
protocol FooProtocol = Foo

actor FooActor Foo where
    BazProtocol -> baz
    FooProtocol -> foo
*/

printer a :: (Show a) => [a] -> String
printer a = show a

main = do
    a <- 4 + 5
    print (show a)
    //bs <- ["a", "5"]
    //print (show "5")
    //print (show bs)
   // print (show "a")
    
module Main(main) where

korte = True

data Foo a = { name :: a, korte :: Int, szilva :: Int}
data Foo2 a = { name :: a, korte2 :: Int, szilva2 :: Int}

main = do
    a  <- Foo { name = "name"
            , szilva = 5, korte = 3}
    a <- a { name = "a"}
    case a of
        Foo {name= a, korte= _, szilva= b } -> print "{}" % a
    
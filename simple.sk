module alma.korte where

plus_one  :: Int -> Int
plus_one x = x + plus_one 1 + 1

id a = a

pipetest x = do
 x |> plus_one
 id 5
 id "a"

somefunc :: (Int -> Int) -> Int
somefunc a = a 5


tricky a = do
    b <- a
    b 4
    somefunc b

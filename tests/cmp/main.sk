module Main where

import Std.Util

main = do 
    assert (1 < 2)
    assert (1 <= 2)
    assert (2 <= 2)

    assert (2 > 1)
    assert (2 >= 1)
    assert (2 >= 2)

    assert !(1 > 2)
    assert !(1 >= 2)

    assert !(2 < 1)
    assert !(2 <= 1)